# --- Java / Maven (backend) ---
backend/target/
*.class

# --- Node / Vite (frontend) ---
tests/dr/node_modules/
tests/e2e/node_modules/
tests/ha/node_modules/
tests/load/node_modules/
tests/ui/node_modules/
devidp/node_modules/
collab/node_modules/
indexer/node_modules/
frontend/node_modules/
frontend/dist/
frontend/.vite/

# --- Logs & env ---
*.log
# Every local env file, including the .env.prod the deployment runbook has you
# create with a real database password. The tracked *.example files are the
# only ones that belong in the repository.
.env
.env.*
!.env*.example

# --- OS / editor ---
.DS_Store
.idea/
*.iml
.vscode/

# Written by stack.sh up: which overlays this machine's stack is running.
.stack.env
